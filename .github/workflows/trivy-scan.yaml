name: Trivy Scan

on:
  workflow_run:
    workflows: [Container Build]
    types: [completed]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.workflow_run.id }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  REGISTRY: ghcr.io

jobs:
  scan:
    if: github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: read
      security-events: write
    strategy:
      matrix:
        variant: [alpine, debian]
    runs-on: ubuntu-24.04
    steps:
      - name: Log in to GHCR
        run: echo '${{ secrets.GITHUB_TOKEN }}' | buildah login -u '${{ github.actor }}' --password-stdin '${{ env.REGISTRY }}'

      - name: Scan ${{ matrix.variant }} image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ github.repository }}:latest-${{ matrix.variant }}
          format: sarif
          output: trivy-results-${{ matrix.variant }}.sarif

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@c4dd10e44af883a891fe31ced449bcb4a6728b9b # v3.37.6
        with:
          sarif_file: trivy-results-${{ matrix.variant }}.sarif
